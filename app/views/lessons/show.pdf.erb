<h1><%= @topic.name %> (<%= @lesson.level %>)</h1>

<%= html(@lesson.topic.description) %>

<% if @lesson.objective? %>
  <div class="objective content">
    <h3><%= t("Learning Objectives") %></h3>

    <%= html(@lesson.objective) %>
  </div>
<% end %>

<% if @lesson.prerequisites? %>
  <div class="prereqs">
    <h3><%= t("Prerequisites") %></h3>

    <div class="content">
      <%= html(@lesson.prerequisites) %>
    </div>
  </div>
<% end %>

<div class="meta">
  <% if @lesson.instructor_students_ratio.present? %>
    <div class="ratio">
      <h3><%= t("Ratio") %></h3>
      <div class="instructors-to-learners"><%= t("Instructor: Learners") %></div>
      <span>
        <%= @lesson.instructor_students_ratio %>
      </span>
    </div>
  <% end %>

  <% if @lesson.suggested_materials? %>
    <div class="suggested-materials">
      <h3><%= t("Suggested Materials") %></h3>

      <div class="content">
        <%= html(@lesson.suggested_materials) %>
      </div>
    </div>
  <% end %>


  <% if @lesson.relevant_articles? %>
    <div class="articles">
      <h3><%= t("Relevant Articles") %></h3>
      <%= html(@lesson.relevant_articles) %>
    </div>
  <% end %>
</div>

<% if @lesson.notes? %>
  <div class="notes content">
    <%= html(@lesson.notes) %>
  </div>
<% end %>

<div class="content">
  <% if @lesson.recommended_reading? %>
    <div class="recommended-reading content">
      <h3><%= t("Recommended Reading") %></h3>
      <%= html(@lesson.recommended_reading) %>
    </div>
  <% end %>

  <h2><%= t("Lesson Content") %></h2>

  <%= html(@lesson.body) %>
</div>
