<div class="row">
  <h1>Security News</h1>

  <%= render "tags/nav", tags: @tags %>

  <% cache(cache_key_for_blog_posts(@blog_posts)) do %>
  <%= render @blog_posts %>
  <% end %>

  <%= paginate @blog_posts %>
</div>
