window.session = {
  lessonPlanId: <%= session[:lesson_plan_id].to_json %>,
  lessonPlanSize: <%= current_lesson_plan.lessons_count %>,
  lessonPlanLessons: <%= current_lesson_plan.lesson_ids.to_json %>
};
