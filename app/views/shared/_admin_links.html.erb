<% if current_admin_user && !@preview %>
  <div class="admin-links">
    <div class="edit">
      <%= link_to t("Edit"), [:edit, :admin, resource] %>
    </div>

    <% if resource.try(:unpublished?) %>
      <div class="unpublished"><%= t("Unpublished") %></div>
    <% end %>
  </div>
<% end %>

