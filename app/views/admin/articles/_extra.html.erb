<%= semantic_form_for(article, url: admin_article_path(article)) do |f| %>
  <%= f.inputs do %>
    <% f.input :slug, hint: "Leave blank to auto-generate" %>
  <% end %>

  <%= f.actions %>
<% end %>
