<div class="topic card">
  <h3><%= link_to topic.name, topic %></h3>
  <div class="tags"><%= tags(topic) %></div>
  <div class="duration"><%= duration(topic.lessons.take.try(:duration)) %></div>
</div>
